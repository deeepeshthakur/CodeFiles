#include <bits/stdc++.h>

using namespace std;

int returnValidStart( int m, int k, std::vector < std::string > &board)
{
  int n = board.size();
  for()
}

int coinOnTheTable( int m, int k, std::vector < std::string> board)
{
  int ans = 0;

  return ans;
}

int main()
{
  int N, M, K;
  std::cin >> N >> M >> K;
  std::vector < std::string > board(N);
  for( int index = 0; index < N; index++)
    std::cin >> board[index];
  int ans = coinOnTheTable( M, K, board);
  std::cout << ans << "\n";
  return 0;
}
