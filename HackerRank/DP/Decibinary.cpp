#include <bits/stdc++.h>

using namespace std;
using integer = unsigned long long;

integer decibinaryNumbers(integer x) {
    // Complete this function
    
}

int main() {
    int q;
    cin >> q;
    for(int a0 = 0; a0 < q; a0++){
        integer x;
        cin >> x;
        integer result = decibinaryNumbers(x);
        cout << result << endl;
    }
    return 0;
}
